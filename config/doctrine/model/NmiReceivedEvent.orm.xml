<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <mapped-superclass name="JpmMartin\SyliusNmiPlugin\Entity\NmiReceivedEvent" table="jpm_martin_sylius_nmi_received_event">
        <unique-constraints>
            <!--
                The whole point of the table. A repeated delivery is refused by the database rather
                than noticed by a query, because two simultaneous deliveries both pass a
                select-then-insert and the gateway retries for three days.
            -->
            <unique-constraint name="uniq_jpm_martin_sylius_nmi_received_event_id" columns="event_id" />
        </unique-constraints>

        <indexes>
            <!-- What the prune scans. Without it, retention gets slower as the table it exists to
                 bound gets bigger. -->
            <index name="idx_jpm_martin_sylius_nmi_received_event_received_at" columns="received_at" />
        </indexes>

        <id name="id" column="id" type="integer">
            <generator strategy="AUTO" />
        </id>

        <!-- A UUID in every delivery observed and in every sample the gateway publishes; 64 leaves
             room for it to be something else without a migration. -->
        <field name="eventId" column="event_id" type="string" length="64" />
        <field name="eventType" column="event_type" type="string" length="64" />
        <field name="paymentMethodCode" column="payment_method_code" type="string" length="255" />
        <!-- Text, not string: a settlement or chargeback batch carries an entry per transaction and
             has no bound this plugin can assert. -->
        <field name="payload" column="payload" type="text" />
        <field name="receivedAt" column="received_at" type="datetime_immutable" />
    </mapped-superclass>
</doctrine-mapping>
