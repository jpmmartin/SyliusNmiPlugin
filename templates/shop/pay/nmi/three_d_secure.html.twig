{#
    Where a 3-D Secure challenge is rendered. The component shows one in a modal by default, so
    this element is usually invisible — it exists because the component needs somewhere to attach,
    and because a store that would rather render the challenge in place can move it.
#}
<div id="nmi-three-d-secure" {{ sylius_test_html_attribute('nmi-three-d-secure') }}></div>
